# CLOSURE_TABLE — correctness closure before the final Lava-host run

Rule: no CURRENT CRITICAL or HIGH may survive into the final host run. MEDIUM: fix when it touches correctness, safety, evidence honesty or genericness; otherwise justify the deferral here. Status values: OPEN → FIXED (code) → TESTED (regression) → VERIFIED (independent re-check / runtime evidence) → CLOSED; or DEFERRED (with justification) / NOT-A-DEFECT (with proof).

Sources: `reports/REVIEW_FINDINGS.md` (internal fresh-Fable review, C/H/M/L ids), `Claude outputs/LAVA_ADVERSARIAL_REVIEW.md` (external hiring-panel adversarial review, "Make The Sensor Lie" items #1–#14 + budget arithmetic), `reports/PONYTAIL_PASS2.md` (F1–F5), `reports/IMPLEMENTATION_NOTES.md` (deviations). Fix batch 1 (checkpoint 13) already closed C1/C2 at code+test level; runtime evidence on the host is pending the final run.

| # | Finding (source ids) | Sev | Root cause | Fix | Regression test | Runtime / Docker evidence | Status |
|---|---|---|---|---|---|---|---|
| 1 | Evidence must entail the verdict — roster-wide (external §MSL preamble; H1/#1/#2/#4/#10 are instances) | CRITICAL (class) | No mechanical link between prose completeness claims ("enumerated successfully", "every enumeration completed", "proven absent") and observation statuses; each check hand-writes its detail | Engine-level gate in `internal/scan/entailment.go`: `completenessOf()` GENERATES the completeness account from the observations and `finalize` attaches it as `evidence.completeness`; `claimsCompleteness()` holds the closed phrase list, and a detail that claims completeness the observations do not support is downgraded to `unknown` and flagged `entailment_violation`. Runs BEFORE the load-bearing downgrade (which now appends rather than replaces). `Observation.AbsenceProven` separates "ENOENT is the answer" from "ENOENT is a gap"; `Result.LoadBearingIf()` marks per branch so a positive finding is not downgraded. `scan.AuditEntailment()` applies the same rule to a finished artifact. | `TestEvidenceEntailsVerdict` (roster x profiles A/B/C + 2 adversarial trees), `TestEntailmentGateDowngradesAnOverclaim`, `TestEntailmentAuditorOnShippedArtifacts` | Run predicate as data over `findings.profile{A,B,C}.json` and the final host artifact | TESTED |
| 2 | PRIVATE_KEY_MATERIAL_EXPOSURE passes with unreadable dirs; `WalkResult.Complete()` ignores UnreadableDirs (H1, #1, M4) | HIGH | Complete() considers only the root error | `WalkResult.Complete()` false on UnreadableDirs / CrossedMounts / budget; `Boundary()` renders why; the walk observation carries `Truncated` so `finalize` acts on it. Check: adverse found => fail even when incomplete; no adverse + boundary => unknown with the boundary list and EACCES vs BUDGET distinguished. | `TestPrivateKey_UnreadableSubtreeIsUnknown` (also asserts the two secrets checks agree on the same directory), `TestPrivateKey_AdverseFindingSurvivesAnIncompleteWalk` | Host: expected unknown (/root EACCES); Docker C: unknown | TESTED |
| 3 | PRIVATE_KEY_MATERIAL_EXPOSURE passes after zero enumerations (#2) | HIGH | pass branch does not require ≥1 inspected root | Zero inspected roots => `unknown`/ENOENT ("nothing was searched"); a root that is absent is recorded with `AbsenceProven` and is not a boundary. | `TestPrivateKey_ZeroWalksIsNotPass` | Docker C (all roots unreadable) | TESTED |
| 4 | HOST_FIREWALL_STATE confident FAIL when ruleset denied / unknown implementation (#3, M1, #13) | HIGH | ruleset EACCES/EPERM/EXEC_ERROR treated as "no filtering"; `ENABLED=no` read and ignored; only a small candidate-unit list | Readable `ENABLED=no` while the unit is active => `fail` citing file and value (the unit is a oneshot); ruleset unreadable => `unknown`; unrecognised implementation => `unknown` naming the blind spot, never fail. The runner maps a tool's own "Permission denied"/"you must be root" stderr to EACCES/EPERM. | `TestFirewall_DeniedRulesetIsUnknown`, `TestFirewall_UfwDisabledIsFail`, `TestFirewall_UnknownImplementationIsUnknown`, `TestExecPrivilegeDenialIsClassifiedAsEACCES` | Host: ufw ENABLED=no readable ⇒ fail; ruleset unreadable noted | TESTED |
| 5 | BMC checks claim "all enumerated successfully" over ENOENT; `!found` wins before `undetermined`; EACCES read as absence (#4, #10) | HIGH | branch ordering + prose not derived from observation statuses | Absence requires every load-bearing listing OK (was an OR over three); `undetermined` now precedes `!found` in BMC_DEVICE_NODE_ACCESS; completeness prose comes from the engine. | `TestBMC_ENOENTNotOverclaimed`, `TestBMC_DeniedPathIsNotAbsent` | Docker B/C; host (node present, root-only) | TESTED |
| 6 | SSH: `sshd -G` labelled "running daemon"; confident policy verdicts while IN_FORCE says stale (#6, M3, external TDA critique) | HIGH | source label wrong; policy checks ignore the in-force result; Env caches raw output (allowed) but the conclusion "running" is not proven | Label is "effective configuration as sshd would load it from disk now". `daemonStateOf()` gathers RAW in-force facts (unit start, newest mtime, measured resolution, relation); ROOT_LOGIN and AUTH_METHODS carry them as `daemon_state`, downgrade to unknown/CONTESTED when the chain is provably newer, and attach a NON-load-bearing caveat when undecidable. Env caches only the raw `systemctl show` observation. | `TestSSHPolicy_SourceLabelIsOnDisk`, `TestSSHPolicy_StaleConfigDowngradesPolicyVerdict`, `TestSSHPolicy_UndecidableOrderKeepsVerdictWithCaveat` | Host: in-force undecidable (same second) ⇒ policy checks carry the caveat | TESTED |
| 7 | SSH_POLICY_IN_FORCE claims 100 ms accuracy it does not have (#7) | MEDIUM (correctness) | monotonic-to-wall reconstruction via /proc/uptime mixes clock domains; resolution asserted, not measured | Resolution is measured, not asserted: two `/proc/uptime` reads bracket the wall sample to measure read skew, and the reconstruction is cross-checked against systemd's rendered timestamp - agreement inside its whole second bounds the clock-domain divergence, disagreement BECOMES the resolution. The fixed 100 ms floor is gone. | `TestInForce_ResolutionIsMeasuredNotAssumed` | Host artifact shows `comparison_resolution_ms` ≥ measured | TESTED |
| 8 | `Include` inside a `Match` block loses Match scope (#8) | MEDIUM (correctness) | walker flattens included directives into global scope | `parseFile` takes the inherited Match scope and passes `currentMatch` into the Include recursion; `Match all` returns to the inherited scope, not to global. | `TestSSHDConfig_IncludeInsideMatchStaysConditional` | fixture | TESTED |
| 9 | Truncated Include glob expansion not recorded / never load-bearing (#9) | MEDIUM (honesty) | glob expansion cap silently drops files | A truncated Include expansion sets `LoadBearing` on its listing observation and marks the chain truncated, so `finalize` downgrades any verdict built on the partial chain. | `TestSSHDConfig_TruncatedGlobIsLoadBearing` | fixture | TESTED |
| 10 | `resolveSocketOwners` unbounded (PID×FD traversal; context never checked) (#5, external determinism note) | HIGH (boundedness) | bound lives only in the caller's context | `resolveSocketOwners` takes a context, checks it per PID, and carries its own budget (2048 processes / 20000 syscalls / 2 s). The boundary is emitted as `owner_attribution`; listeners not reached are reported unattributed, not unowned. | `TestSocketOwners_BudgetIsEnforcedAndVisible` | Docker A run shows bounded time; evidence field present | TESTED |
| 11 | Audit other host-access paths for caller-only bounds (external §Boundedness) | MEDIUM (safety) | same class | Audited: directory listings are capped at the call site, `Walk` carries entry/depth/time budgets and xdev, the sshd Include chain is capped by depth and file count, and the socket-owner pass gained the budget above. No unbounded host-access loop remains. | covered by `TestSocketOwners_BudgetIsEnforcedAndVisible`, `TestWalkBoundaryCountersSurviveEvidenceCap`, `TestReadDirBudgetIsRecorded` | — | TESTED |
| 12 | Budget arithmetic: per-check budgets sum ≈271 s vs 60 s scan deadline, sequential (external) | MEDIUM (correctness) | per-check budgets sized independently | Arithmetic documented in `scan.Run`: budget = min(declared, remaining scan deadline), so the deadline dominates and a slow early check cannot starve a later one - every later check still emits its own finding with BUDGET_EXHAUSTED or TIMEOUT. | `TestScanDeadline_DominatesPerCheckBudgets` | Host runtime ≈ 88 ms (far below) — evidence | TESTED |
| 13 | `resolveBinary` falls back to inherited PATH (L2, #12) | MEDIUM (safety) | LookPath fallback | `resolveBinary` resolves only from the fixed absolute list; the `exec.LookPath` fallback is gone. Not found => UTILITY_MISSING whatever PATH says. | `TestResolveBinary_IgnoresPATH` (plants a fake `mokutil` on PATH and asserts it is not run) | Docker C | TESTED |
| 14 | ACL read follows symlinks while comment claims otherwise (L1, #11) | MEDIUM (honesty) | Getxattr vs Lgetxattr | `lgetxattr(2)` issued directly (Go's `syscall` wraps only the following variant); comment corrected to state what the primitive does. | `TestReadACL_DoesNotFollowFinalSymlink` | — | TESTED |
| 15 | MEDIA_HEALTH_VISIBILITY: reason hardcoded; children (nvme/smartctl) open device nodes (M2, external customer-safety note) | MEDIUM (safety/honesty) | exec of tools that open devices; reason not derived | `nvme smart-log` and `smartctl -H` removed entirely - both make a CHILD open the device and issue an admin passthrough. Reachability from metadata: node mode/owner/group vs euid + `/proc/self/status` groups, tool inventory, sysfs signals. Reason derived: EACCES when not openable, new `NOT_ATTEMPTED` when openable and declined by design. Evidence carries `device_opened_by_sensor` and `device_opened_by_child_process`. | `TestMediaHealth_NoDeviceOpeningChildren`, `TestMediaHealth_ReasonDerivedFromObservations` | Host: unknown/EACCES with sysfs signals | TESTED |
| 16 | Goroutine panic containment outside runOne (external safety) | MEDIUM (safety) | `ReadOOB` goroutine and any other background goroutine lack recover | `recover()` in the `ReadOOB` goroutine and in the capped-writer overflow kill goroutine; a panic there would take the process down past `runOne`. | `TestOOBGoroutinePanicDoesNotKillProcess` | — | TESTED |
| 17 | Walk: incomplete/unreadable boundaries must stay visible when evidence arrays hit caps (external) | MEDIUM (honesty) | evidence array cap can drop boundary entries | `unreadable_dirs_count` / `dirs_pruned_count` are always emitted and `boundary_lists_truncated` is set when a list is capped, so a boundary dropped from a capped list does not vanish. | `TestWalkBoundaryCountersSurviveEvidenceCap` | Docker C | TESTED |
| 18 | Profile/check matrix uses t.Logf on mismatch (external, item 8) | HIGH (test gate) | contract mismatch not a failure | matrix test FAILS on mismatch; expectations = registry §5 (corrected) | `TestProfileMatrix_ProfileA_HardGate` (13/9/4 — registry's 12/9/5 plus one documented deviation, CREDENTIAL_FILE_EXPOSURE pass via batch-3's protectionFromDenial()), `TestProfileMatrix_ProfilesBC_HardGate` (B: 14 rows widened, by-design minimal image; C: 3 coordinator-directed corrections + 2 more of the same /root-shielding/AbsenceProven class) | **CLOSED, verified against checkpoint 16 (batch 3).** Rebuilt `bin/sensor` + all Linux test binaries from HEAD; gate run 3x back-to-back under WSL uid 1000 — identical 13/9/4 for A and identical PASS/PASS for B/C every time; whole `internal/lab` suite 3x identical PASS. `go test ./internal/lab/...` clean on Windows (the four `_modes.txt`-dependent tests correctly SKIP there — H3's Windows-nondeterminism fix from the previous update, re-verified still in effect) and on WSL (`checks`, `probe`, `scan`, `cmd/sensor` binaries all PASS). `GOOS=linux staticcheck ./internal/lab/...` clean. See TEST_REPORT.md §13 | CLOSED |
| 19 | Docker profile C not hostile (identical to A) (external, item 9) | HIGH (test gate) | only PATH changed; resolveBinary ignores PATH | rebuild C: unreadable dirs (/etc/ssh, /etc/ssl/private, /var/lib/cloud, /run/udev), absent utilities, uid with no passwd/home, masked sysfs surfaces, restricted proc where possible; assert findings | `tooling/testlab/assert_profile.py` + Dockerfile.profileC | **CLOSED.** Re-run on the checkpoint-16 binary via `run_in_docker.sh` (assertions wired): A 10/2/14 (243ms), B 10/2/14 (75ms), C 5/1/20 (144ms) — `assert_profile.py` **0 violations on all three** (G1 schema, G2 26/26 roster, G3 per-profile status incl. two new Docker-specific structural-unknown rows for BMC_INBAND_INTERFACE_PRESENT/BMC_HOST_INTERFACE_EXPOSURE, G4 entailment). A≠C mechanically confirmed: 6/26 check_ids flip status outright plus the distribution shift. Entailment audit (G4 logic) also run directly over `sensor/bin/findings.wsl.json`: 0 violations. `/proc/net` and `/sys/class/dmi` tmpfs masking remains NOT achievable under runc without CAP_SYS_ADMIN (documented structural limitation, `--network none` substitutes for the /proc/net intent) | CLOSED |
| 20 | Registry summary arithmetic (H2) | HIGH (docs) | prose not derived from table | corrected 12/8/5 (+BOOT_KERNEL_DRIFT ⇒ 12/9/5) | n/a | compare final host run | CLOSED (lead, 02:20Z) |
| 21 | CREDENTIAL_FILE_EXPOSURE system-account homes / duplicates (L3, batch-1 #5) | LOW | scope | login-capable accounts + root; dedupe; skip system dirs | `TestCredentialHomes_SkipsSystemDirsAndDeduplicates` | host: unknown (root EACCES) | TESTED (batch 1) |
| 22 | PRIVATE_KEY candidates include 121 CA-bundle symlinks (L4) | LOW | symlinks counted as candidates | Symlinks and other non-regular entries are never candidates; counted as `symlinks_skipped` / `non_regular_skipped`. | `TestPrivateKey_SymlinksAreNotCandidates` | host artifact | TESTED |
| 23 | Ponytail F1 Files interface with one impl; F3 `--timeout` flag violates LD-9 and widens deadline; F5 dead decls / triplicated regexp | MEDIUM (scope/LD-9) | — | F1 `probe.Files` deleted, `Env.Files` is `*probe.Reader`, ModTime cast gone. F3 `--timeout` removed, `DefaultScanDeadline` stays a constant. F5 `probe.Which`, `Observation.Failed`, `KindConfigRes`, `KindDeviceProbe`, `ReasonENODEV` deleted; one compiled UPPER_SNAKE_CASE regexp. `Budgeted` folded into `Check.Budget()`. | `TestExitCodeContract` ("no flag may widen the scan deadline"), whole-suite build | — | TESTED |
| 24 | Ponytail F2 lab fixture duplication; F4 !unix stub | LOW | — | F2 DEFERRED (independence choice; another day); F4 documented as deviation 14 (only linux/amd64 ships) | — | — | DEFERRED / DOCUMENTED |
| 25 | C1 SSH_POLICY_IN_FORCE same-second false FAIL | CRITICAL | second-resolution comparison | batch 1: monotonic + TIMESTAMP_RESOLUTION | `TestSSHPolicyInForce_SameSecondIsUndecidable` (+3) | pending final host run (expected unknown) | TESTED |
| 26 | C2 PROVISIONING_DATA_PROTECTION false FAIL on redacted/public artifacts | CRITICAL | no payload/public class split | batch 1: payload-class rule | `TestProvisioning_PublicByDesignArtifactsAreNotExposure` (+3) | pending final host run (expected pass) | TESTED |
| 27 | Batch-1 #1 diskless root FAIL / #2 smartctl substring | HIGH | — | batch 1 | 2+4 tests | fixtures | TESTED |

| 28 | R2-C1: PRIVATE_KEY candidates whose 64-byte sniff returns EACCES are silently dropped → pass with "0 candidates classified" (REVIEW_FINDINGS_2 C1) | CRITICAL | exposure question treated a denied content read as "not a candidate" | Exposure semantics: a denied read of a candidate is evidence about protection, never a reason to drop it. `classifyHeader` failure keeps the hit as `unclassified (<errno>)` and the verdict is taken from mode/owner/group; `protectionFromDenial()` walks to the nearest stat-able ancestor and, where its mode excludes other unprivileged accounts, records the candidate as protected and opts the denial out of the load-bearing set with the reason recorded. | `TestPrivateKey_UnreadableCandidateIsJudgedFromItsMode` (R2-A), and after batch 4 `TestR3B_UndeterminedGroupModelIsUnknownNotFail` — an unread candidate whose reader set cannot be resolved is `unknown`, not a silent pass | Docker C; WSL uid 1000 (batch-4 binaries) | TESTED |
| 29 | R2-C2: "group empty ⇒ owner-only" counts only /etc/group members, ignores primary gid in /etc/passwd (secrets.go, bmc.go, storage.go) | CRITICAL | group membership model incomplete | `Env.Groups()` parses /etc/passwd as well and adds every account to the effective member set of its PRIMARY group. Batch 4 completed the model: `Env.Readers(mode, uid, gid)` SUBTRACTS the owner, and `Readers`/`Traversers`/`Accessors` are three wrappers over one `beyondOwner()`, so secrets.go, bmc.go and the credential and secret-store checks cannot disagree about the same file. Correction to this row as written: `storage.go`'s `openableByUs()` deliberately does NOT use it — it answers a different question ("could THIS identity open it", via our own supplementary groups) and is documented as such in IMPLEMENTATION_NOTES (L4). | `TestEffectiveReaders_PrimaryGidCounts` (R2-B), `TestReadersSubtractsTheOwner`, `TestR3L4_BMCNodeUsesTheSharedAccountModel` | fixtures; WSL uid 1000 | TESTED |
| 30 | R2-C3: SYSTEM_SECRET_STORE_PROTECTION fails stock Debian/Ubuntu `/etc/sudoers.d` 0755 with 0440 files | CRITICAL | directory mode judged instead of the files' readability | A directory store is judged by the FILES inside it: the directory is listed, each child inspected, and only a readable secret (or an other-WRITABLE directory) fails. The traversable rule is gone. | `TestSecretStore_StockSudoersDIsNotExposure` (R2-D2), and after batch 4 `TestR3C_OwnerIsNotAnotherReaderOfItsOwnFile` + `TestR2D2_StockSudoersDDoesNotFail` — the 0440 files inside it are owner-only, which is what row 42 fixed | Docker A/B (stock images); WSL uid 1000 | TESTED |
| 31 | R2-C4: CREDENTIAL_FILE_EXPOSURE rule says group/world-WRITABLE but code tests read bits → `~/.ssh/config` 0644 fails | CRITICAL | predicate/rule text mismatch | `ruleKind` generates the rule TEXT and the PREDICATE from one declaration: ssh config on writability, .pgpass/.netrc/.my.cnf/.git-credentials/docker/kubeconfig on readability. | `TestCredentialRules_PredicateMatchesRuleText` (R2-C) | fixtures | TESTED |
| 32 | R2-H1: entailment gate bypass — pass with zero load-bearing observations + unlisted absence prose passes (LoadBearingIf opt-in; phrase blacklist) | HIGH | load-bearing is opt-in; prose check is a blacklist | Observations are load-bearing BY DEFAULT; opting out requires a reason that ships as `not_load_bearing_because`. pass/fail with no successful load-bearing observation => unknown/NO_EVIDENCE (new closed-vocabulary reason). Absence prose is matched by regexp over the whole class, not a phrase list; the completeness sentence is generated. | `TestEntailment_UnlistedAbsenceProseIsCaught` (R2-G), `TestEntailment_VerdictWithoutEvidenceIsNoEvidence`, `TestEntailment_OptOutRequiresARecordedReason`, roster-wide `TestEvidenceEntailsVerdict` | audit over A/B/C + host artifact | TESTED |
| 33 | R2-H2: unreadable /etc/group ⇒ every group "empty" ⇒ false pass | HIGH | group DB failure treated as empty set | An unreadable /etc/group or /etc/passwd makes group-based reasoning UNDETERMINED, never "every group is empty". Batch 4 corrected the second half of this row: undetermined no longer means "conservatively readable beyond owner" — asserting an exposure the sensor cannot know is as wrong as asserting safety — it means the affected candidate is `unknown`, and every consumer branches on `Determined`. | `TestGroupDBDenied_IsUnknownNotEmpty` (R2-D), `TestR3B_UndeterminedGroupModelIsUnknownNotFail`, `TestUndeterminedGidIsNeitherExposureNorSafety` | Docker C (no passwd/group context); WSL uid 1000 | TESTED |
| 34 | R2-H3: lab hard gate red on current source while TEST_REPORT claims green; profile A distribution non-reproducible (5/1/20 vs 9/9/8 vs 10/9/7) | HIGH | fixture materialisation nondeterminism suspected; report/state drift | Root cause was NOT fixture nondeterminism: (a) `os.Chmod` is a no-op on Windows for the `_modes.txt` permission bits `TestProfileMatrix_ProfileA_HardGate`/`...ProfilesBC_HardGate`/`TestProfileCNeverPassesOrFailsWhatItCannotObserve` depend on, so Windows silently produced a different, wrong-but-not-"nondeterministic" distribution — fixed with `requireLinux`/`skipIfRoot` guards (existing codebase convention), Windows now SKIPs these tests cleanly; (b) the *remaining* value-to-value differences the reviewer saw (5/1/20, 9/9/8, 10/9/7, 12/9/5, 13/9/4) were the implementation author's batch-2/3/4 commits landing live in the working tree between successive `go test -c` compiles during concurrent work, not test-harness flakiness — proven by rebuilding once and re-running 3x, which is always internally identical | `TestProfileMatrix_ProfileA_HardGate` + `TestProfileMatrix_ProfilesBC_HardGate`, 3 consecutive runs under WSL uid 1000 against one fixed `bin/lab/lab.test`: identical 13/9/4 (A) and identical PASS/PASS (B/C) every time, verified repeatedly across checkpoints 16/17/18; `go test ./internal/lab/...` clean on Windows (four tests SKIP, none FAIL) | WSL uid 1000, checkpoint 18 (`a17501d`), `bin/sensor` sha256 `2e338db9…5ee75960f9818c` | CLOSED |
| 35 | R2-H4: PROVISIONING_DATA_PROTECTION unknown on any host with 0700 /root because /root/anaconda-ks.cfg is EACCES | HIGH (under-claim) | denied read of a payload candidate treated as a gap | Exposure semantics applied to provisioning: a payload path denied behind an ancestor no unprivileged account can traverse is counted as protected and the denial is opted out with its reason. Expected host result: pass. | `TestProvisioning_UnreadableRootDirIsProtectionNotAGap` (R2-E2), and after batch 4 `TestR3D_RootDir0550IsStillProtection` (the RHEL 0550 shape) + `TestProtectionOptOutTextIsSharedByEveryExposureCheck` | host: expected pass; WSL uid 1000 | TESTED |
| 36 | R2-M: truncated /proc/net/tcp hides a telnet listener → unknown/BUDGET_EXHAUSTED | MEDIUM (under-claim) | truncation ⇒ whole check unknown | A listener SEEN in the rows that were read is a positive observation: the table completeness is opted out when one is found, and a truncation with nothing adverse is unknown/BUDGET_EXHAUSTED naming how many rows were read. | `TestListeners_TruncatedTableKeepsPartialEvidence` (R2-F), `TestListeners_TruncationWithoutAdverseIsUnknown` | fixture | TESTED |
| 37 | R2-M: BMC_DEVICE_NODE_ACCESS downgrades a proven absence with self-contradicting detail (no AbsenceProven) | MEDIUM | ENOENT of the node after an OK listing of /dev not marked as answer | `probe.Reader` marks `AbsenceProven` on ENOENT whenever an ancestor stats successfully, so a /dev/ipmi* ENOENT after a listable /dev is an answer; the BMC node stats inherit it and the detail is generated. | `TestBMCNode_ProvenAbsenceIsAnAnswer` (R2-I) | Docker B | TESTED |
| 38 | R2-M: cloud.cfg.d comment containing "password" ⇒ payload promotion ⇒ false FAIL | MEDIUM | key-name matcher scans comments | `declaredKeys()` strips `#` comments and matches a key only at line start followed by a colon, so a comment mentioning ssh_pwauth or password no longer promotes a public drop-in to payload. | `TestProvisioning_CommentIsNotACredentialKey` (R2-E), `TestDeclaredKeys` | fixture | TESTED |
| 39 | R2-M: registry expectation PRIVATE_KEY=pass on the host; honest result is unknown (/root unreadable) | MEDIUM (docs) | registry error | Superseded by batch 3 semantics: exposure question ⇒ denial is protection ⇒ expectation stays pass with the shielded subtree reported (registry row re-confirmed 04:35Z) | matrix expectation (lab rerun) | host | CLOSED (lead) |
| 40 | R2-M: a walk root on a hung NFS home blocks the scan; no output written | MEDIUM (safety/bounds) | check Run() cannot be interrupted in a blocking syscall; engine waits | The engine runs each check in its own goroutine and ABANDONS it at its deadline (buffered channel, never joined), emitting unknown/TIMEOUT and continuing, so the document is written even when a check is stuck in an uninterruptible syscall. Walks refuse to START on a network filesystem (nfs/nfs4/cifs/smb3/fuse.sshfs/afs/9p/ceph/gluster/lustre) with the skipped root and fstype recorded. | `TestEngine_StuckCheckIsAbandonedAndOutputIsStillProduced` (a check that ignores its context for 30 s), `TestWalk_RefusesToStartOnNetworkStorage` | fixture with a blocking reader | TESTED |
| 41 | R2-M/L: wrong reason class in BMC tooling inventory; other Lows (see REVIEW_FINDINGS_2) | LOW | — | BMC tooling inventory takes its reason from the directory stats via `reasonOf` and names the directories instead of an empty list. L1: a policy-refused symlink is classified ELOOP. L2: `passwd -S` documented in IMPLEMENTATION_NOTES and opted out of the load-bearing set. L3 belongs to internal/lab. | `TestBMCTooling_ReasonComesFromTheStats`, `TestSymlinkRefusalIsInTheClosedVocabulary` | — | TESTED |

| 42 | R3-C1: Readers() counts the owner as an effective member of its own primary group → 0440 root:root judged "reachable beyond owner" → false FAIL (SYSTEM_SECRET_STORE, and every mode-based check) | CRITICAL | owner not subtracted from the group set; primary-gid expansion added the owner back | `Env.Readers(mode, uid, gid)` takes the owner uid and subtracts the owner from the effective member set; "beyond owner" is `(group members ∪ primary-gid holders) \ {owner}`. `GroupDB.OwnerName(uid)` provides the name. `Readers`, `Traversers` and `Accessors` are three wrappers over one `beyondOwner()`, so every mode-based check inherits it. | `TestR3C_OwnerIsNotAnotherReaderOfItsOwnFile`, `TestReadersSubtractsTheOwner`, `TestR2D2_StockSudoersDDoesNotFail` | WSL uid 1000: R3-C SECRET_STORE pass, PRIVATE_KEY pass. Docker A with sudo + host (/etc/sudoers 0440) still pending | TESTED |
| 43 | R3-H1: undetermined group model (passwd/group unreadable, SSSD gid not in files) → fail/POLICY with a detail saying unknown | HIGH | callers ignore Determined=false | `Determined=false` no longer forces `BeyondOwner`; it asserts nothing. PRIVATE_KEY collects `candidates_with_an_unresolved_reader_set` and returns unknown naming them; CREDENTIAL_FILE, PROVISIONING and SYSTEM_SECRET_STORE route the same condition into their boundary lists. Never a fail whose detail says unknown, never a pass. | `TestR3B_UndeterminedGroupModelIsUnknownNotFail` (both databases × both checks), `TestUndeterminedGidIsNeitherExposureNorSafety` (SSSD/LDAP gid) | WSL uid 1000: SECRET_STORE unknown/EACCES, PRIVATE_KEY unknown/ENOENT. Docker C pending | TESTED |
| 44 | R3-H2: protectionFromDenial tests read bits, not the directory traverse (x) bit; a 0710 dir root:ssl-cert with members counts as protected | HIGH | wrong permission bit for directories | `protectionFromDenial()` asks `Env.Traversers()` (bits 0o001/0o010), not the read bits: an ancestor shields only when nobody beyond its owner can pass through it. Other-x, or group-x with a member beyond the owner, ⇒ NOT protected ⇒ the candidate stays unknown-exposure. The ancestor stats that decided it are recorded as observations. | `TestR3A_GroupTraversableAncestorIsNotProtection`, `TestTraversersUsesTheExecuteBitNotTheReadBit` (0710 traversable but not readable; 0700 neither) | WSL uid 1000: R3-A `protected_by_ancestor` = 0, boundary names postgres. Docker A with an `ssl-cert` member pending | TESTED |
| 45 | R3-H3: SYSTEM_SECRET_STORE_PROTECTION counts denials as protection but never opts them out of load-bearing → permanent unknown/EACCES "downgraded from pass" on every Ubuntu | HIGH | protection accounting not wired to the opt-out with reason | One routine, `recordProtection()`, used by PRIVATE_KEY, CREDENTIAL_FILE, PROVISIONING and SYSTEM_SECRET_STORE: it records the ancestor stats and stamps the denial with `not_load_bearing_because: protection (ancestor <path> mode <octal>, readers beyond owner: none)`. The prefix is the exported `scan.ProtectionOptOut`, which the entailment gate also recognises as the one excuse an absence claim may rest on. | `TestR3I_ProtectedSecretStoreIsPassNotPermanentUnknown` (asserts the opt-out text is present), `TestProtectionOptOutTextIsSharedByEveryExposureCheck` (all three other checks) | WSL uid 1000: SYSTEM_SECRET_STORE_PROTECTION flipped from unknown/EACCES to **pass**; host expected pass | TESTED |
| 46 | R3-M1/M2: walk observations copied before Truncated is finalised (wrong BUDGET reason); unreadable mountinfo downgrades a FOUND world-readable key | MEDIUM | ordering; adverse-found precedence missing in one path | The walk observation is copied into the result only after the per-subtree protection pass has settled `Truncated`. The exposure opt-out now names every completeness source (walk sources + `/proc/self/mountinfo`) and only those, so the stats that PROVE the exposure stay load-bearing and the fail is legal under the entailment gate. | `TestR3E_UnreadableMountTableCannotHideAFoundKey`, `TestWalkObservationIsCopiedOnlyOnceItsTruncationIsFinal` | WSL uid 1000: R3-E fail/POLICY | TESTED |
| 47 | R3-M3: symlinked /home root not walked; reason leaks `ENOTREG` outside the closed vocabulary | MEDIUM | root symlink policy + reason mapping | `Walk` follows a symlinked root one hop when the target is a local directory not writable by other accounts (recorded as `root_symlink_followed`); every other case is refused with one of the six phrases in `probe.WalkSkipReasons` and surfaced as `scan_roots_not_enumerated`. `scan.NormalizeReason()` in `finalize` is the single choke point mapping ENOTREG/ENOTDIR/ENOTSUP/ELOOP/EISDIR/ENXIO/ENODATA to EINVAL and anything unrecognised to INTERNAL_ERROR; `scan.ReasonVocabulary` is the closed set. | `TestR3G_SymlinkedWalkRootIsFollowedOnce`, `TestSymlinkedWalkRootToAWritableTargetIsRefusedInWords`, `TestWalkSkipReasonsAreInTheClosedVocabulary` (drives every refusal path), `TestEveryFindingReasonIsInTheClosedVocabulary` (whole roster), `TestNormalizeReasonMapsEveryInternalToken` | WSL uid 1000: R3-G fail/POLICY naming the key under the symlink target | TESTED |
| 48 | R3-L: NFS refusal reason class (L1); absence-prose bypass defence-in-depth (L2); other Lows per REVIEW_FINDINGS_3 | LOW | — | L1 `Observation.Refused` ⇒ reason NOT_ATTEMPTED for both walk refusal paths. L2 regexp class widened AND the structural gate strengthened: a REFUSED enumeration that was opted out no longer excuses an absence claim (`Completeness.ExcusedEnumerationDenials`), the sole exception being the generated protection opt-out. L3 SSH_POLICY_IN_FORCE takes its reason from the first failing observation. L4 bmc.go uses `Env.Accessors()`; storage.go's `openableByUs()` justified as a different question in IMPLEMENTATION_NOTES. L5 absent credential candidates are recorded with AbsenceProven. L6 mode-bit rules are tested on mode bits. | `TestR3F_NetworkRootRefusalIsNotABudgetExhaustion`, `TestR3H_AbsenceProseIsRejectedHoweverItIsWorded`, `TestSSHPolicyReasonFollowsTheFailingObservation`, `TestR3L4_BMCNodeUsesTheSharedAccountModel`, `TestR3L5_AbsentCredentialCandidatesAreRecorded`, `TestR3L6_ModeBitRulesAreTestedOnModeBits` | WSL uid 1000 | TESTED |
| 49 | Docker gate gap: profile A image lacks `sudo` (so C1 class invisible) and a populated `ssl-cert`-style group (H2 class) | HIGH (test gate) | image not host-shaped for these classes | `Dockerfile.profileA`: installed `sudo` (ships `/etc/sudoers` 0440 root:root, `/etc/sudoers.d` 0755 root:root as the package actually ships it, verified by building — not 0750 as first guessed) plus `/etc/sudoers.d/99-lab-owner-only-fixture` (0640 root:root, the C1 owner-as-sole-group-member contrast case); installed `ssl-cert`, added a real member (`postgres`, system account) to the group, set `/etc/ssl/private` 0710 root:ssl-cert with `lab-dummy.key` (0640 root:ssl-cert, PEM header only, no real key) — the H2/H3 case | `assert_profile.py`'s `EXPECTED_A_DOCKER` overrides for `SYSTEM_SECRET_STORE_PROTECTION`, `PRIVATE_KEY_MATERIAL_EXPOSURE`, and (profile C) `CREDENTIAL_FILE_EXPOSURE` | Rebuilt against checkpoint 18 (`a17501d`, batch 4) and rerun via `run_in_docker.sh`: **Docker A/B/C all 0 violations.** Both new-fixture rows were drafted from a hypothesis first (pass / fail respectively) and then corrected against the REAL observed output rather than shipped as guesses: both are `unknown/EACCES` — batch 4's `Traversers()` fix (row 44) correctly finds the 0710 ancestor does not shield (ssl-cert has a real non-owner member, postgres), but the sensor account (`ubuntu`) is itself in neither the owner nor `ssl-cert` and so can never traverse in to see `lab-dummy.key`'s own mode; asserting `fail` from that would itself be the over-claim CLAUDE.md's evidence semantics forbid, so `unknown` is the correct, honest, and actually-observed verdict for both checks (evidence text quoted in `assert_profile.py` and TEST_REPORT.md). Status counts: A 8/2/16, B 10/2/14, C 8/1/17. Entailment audit + schema: 0 violations, all three plus `bin/findings.wsl.json` | CLOSED |

Closure procedure: author applies fixes → regression suite (Windows + WSL uid 1000) → Staticcheck + Hydra prefilter → Docker A/B/C with mechanical assertions → fresh Fable adversarial reviewer ("make the sensor lie") → fixes → rerun everything → final Lava-host run → this table updated with runtime evidence and statuses → closure audit.

## Fix batch 2 closure note (implementation author, after the batch)

Items 1-17, 22 and 23 are FIXED and TESTED at code level. Gates run after the
batch: `gofmt` clean; `go vet ./...` clean natively and for `GOOS=linux`;
`GOOS=linux staticcheck ./...` clean (rc 0); `go test ./...` on Windows; the four
cross-compiled test binaries under WSL Ubuntu 26.04 as uid 1000 -> **202 passed,
0 failed**; `tooling/validate_findings.py` on the WSL artifact -> RESULT PASS, 0
violations; Docker profiles A, B and C re-run and all three validate PASS. The
entailment rule was then applied in DATA mode to all six current artifacts (WSL +
Docker A/B/C) with no violations; `reports/real_host/findings.json` and
`reports/testlab/findings.wsl.slice.json` predate the gate and are skipped by
name until the final host run regenerates them.

Two items belong to the test-lab owner and were deliberately not touched (18, 19,
and `sensor/internal/lab/`). After batch 2 the lab's profile matrix reports three
expectation mismatches that are behaviour changes, not regressions, and need the
registry §5.3 rows updating by their owner:

| check | profile | registry §5.3 | now observed | why the new behaviour is the honest one |
|---|---|---|---|---|
| `PRIVATE_KEY_MATERIAL_EXPOSURE` | C | unknown | pass | one scan root (`/etc/ssh`) exists and was fully enumerated with no key-class file in it. Item 3 asks for unknown on ZERO roots; one root that was completely read is a real pass, and the generated `completeness` states the scope. If profile C is rebuilt hostile (item 19) this becomes unknown again on its own. |
| `BOOT_ARTIFACT_READABILITY` | C | pass | pass | fixed during this batch: `/boot` ENOENT is now `AbsenceProven` and yields pass ("no boot artifacts on this system to expose"), where it briefly returned unknown. |
| `SSH_ROOT_LOGIN_POLICY` | C | unknown | pass | the lab fixture supplies a stubbed `sshd -G` answering `permitrootlogin no`; with a daemon answering, pass is correct. The registry row assumes no sshd in a container. |

## Fix batch 3 closure note (implementation author)

Rows 28-33, 35-38, 40 and 41 are FIXED and TESTED. The batch turns on one
distinction: for an EXPOSURE question a denial is evidence of protection, and for
an EXISTENCE question it is a gap. Applied to PRIVATE_KEY_MATERIAL_EXPOSURE,
PROVISIONING_DATA_PROTECTION, CREDENTIAL_FILE_EXPOSURE and
SYSTEM_SECRET_STORE_PROTECTION.

Gates after the batch: gofmt clean; `go vet ./...` clean natively and for
GOOS=linux; `GOOS=linux staticcheck ./...` rc 0; Windows `go test ./...` green
for ALL FIVE packages including `internal/lab` (H3/L3 are green in this tree);
WSL Ubuntu 26.04 uid 1000, four cross-compiled test binaries: **220 passed, 0
failed** (probe 19, scan 32, checks 159, cmd 10); WSL scan 26 checks exit 0;
`tooling/validate_findings.py` RESULT PASS on the WSL artifact and on Docker
profiles A, B and C, all regenerated with the batch-3 binary; the entailment rule
re-run in DATA mode over all four with no violations.

Two batch-2 expectations were deliberately superseded and their tests renamed:
a subtree this account cannot enter is protection for an exposure question, not
a gap. What the tests now assert is that the shielding is counted and reported
rather than silently assumed, and that the two secrets checks agree about the
same directory.

Registry reconciliation (M4/H4 in REVIEW_FINDINGS_2) is the lead's: with batch 3
the expected host results for PROVISIONING_DATA_PROTECTION and
PRIVATE_KEY_MATERIAL_EXPOSURE are pass again, which is what CHECK_REGISTRY 2.2
and 5.3 predict, so the sensor-versus-registry disagreement those rows recorded
should close on the final host run rather than needing a registry edit.

## Fix batch 4 closure note (implementation author)

Rows 42-48 are FIXED and TESTED, and rows 28, 29, 30, 33 and 35 were re-verified
against the reviewer's own fixtures rather than only against my tests. Rows 34
and 49 are the lab author's and are untouched here.

One sentence explains the whole batch: **the owner is not somebody else.** The
sensor answered "who can reach this?" by listing the effective members of the
file's group, which on 0440 `root:root` contains `root` — the owner. That single
error produced the Critical (false FAIL on `/etc/sudoers`, which every Debian,
Ubuntu and RHEL host ships, including the target), and two of the three Highs
followed from the same region: an ancestor's READ bit was asked when the
question was TRAVERSAL, and an undetermined account model was reported as an
exposure instead of an open question. The fourth High was the missing half of
row 28's fix: `SYSTEM_SECRET_STORE_PROTECTION` counted denials as protection but
never opted them out, so on Ubuntu it shipped `unknown` "downgraded from pass"
beside a detail saying it had passed. All four now go through one account model
(`beyondOwner`) and one protection routine (`recordProtection`).

Two corrections to earlier rows are recorded rather than quietly applied:

- **Row 29** claimed `Env.Readers()` was used by `storage.go`. It is not, and
  should not be: `openableByUs()` answers "could THIS identity open it" from our
  own supplementary groups, which is a different question. `bmc.go` did have its
  own loop and now uses `Env.Accessors()`. The row text is amended.
- **Row 33** said an undetermined group model should be treated as
  "conservatively readable beyond owner". Batch 4 reverses that half: asserting
  an exposure the sensor cannot know is as wrong as asserting safety. The
  affected candidate is `unknown`, with the reason naming which database was
  unreadable or which gid could not be resolved.

Gates after the batch: gofmt clean; `go vet ./...` clean natively and for
GOOS=linux; `GOOS=linux staticcheck ./...` rc 0; Windows `go test ./...` green
for all five packages; WSL Ubuntu uid 1000, four cross-compiled test binaries
each run from its own package directory: **244 passed, 0 failed, 0 skipped**
(probe 20, scan 32, checks 182, cmd 10). WSL scan: 26 checks, exit 0, 9 pass /
2 fail / 15 unknown; `tooling/validate_findings.py` RESULT PASS with 0
violations; `scan.AuditEntailment` clean over the eight shipped artifacts
(including the new `reports/testlab/findings.wsl.batch4.json`).

The reviewer's fixtures were re-run unmodified from
`reports/review3/sensorcopy/`: R3-A..I, R2-A..I and ADV-1..6 all reach their
expected outcome, with one deliberate exception — ADV-3 (`/root` 0000 hiding a
0644 key) still asserts not-pass, which closure row 39 reversed in batch 3 and
which the reviewer's own verdict table records as "by design since row 39". The
full outcome table is in IMPLEMENTATION_NOTES, "Fix batch 4".

Docker A/B/C were NOT re-run in this batch: the lab author is adding `sudo` and
an `ssl-cert`-style group to profile A (row 49), and rerunning the current images
would re-confirm the same blind spot that let the Critical through three green
gates. The Docker evidence columns for rows 42-45 stay pending on that work.
