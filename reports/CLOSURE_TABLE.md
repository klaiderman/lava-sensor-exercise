# CLOSURE_TABLE — correctness closure before the final Lava-host run

Rule: no CURRENT CRITICAL or HIGH may survive into the final host run. MEDIUM: fix when it touches correctness, safety, evidence honesty or genericness; otherwise justify the deferral here. Status values: OPEN → FIXED (code) → TESTED (regression) → VERIFIED (independent re-check / runtime evidence) → CLOSED; or DEFERRED (with justification) / NOT-A-DEFECT (with proof).

Sources: `reports/REVIEW_FINDINGS.md` (internal fresh-Fable review, C/H/M/L ids), `Claude outputs/LAVA_ADVERSARIAL_REVIEW.md` (external hiring-panel adversarial review, "Make The Sensor Lie" items #1–#14 + budget arithmetic), `reports/PONYTAIL_PASS2.md` (F1–F5), `reports/IMPLEMENTATION_NOTES.md` (deviations). Fix batch 1 (checkpoint 13) already closed C1/C2 at code+test level; runtime evidence on the host is pending the final run.

| # | Finding (source ids) | Sev | Root cause | Fix | Regression test | Runtime / Docker evidence | Status |
|---|---|---|---|---|---|---|---|
| 1 | Evidence must entail the verdict — roster-wide (external §MSL preamble; H1/#1/#2/#4/#10 are instances) | CRITICAL (class) | No mechanical link between prose completeness claims ("enumerated successfully", "every enumeration completed", "proven absent") and observation statuses; each check hand-writes its detail | Engine-level predicate in `scan`: a finding whose detail/evidence asserts completeness/absence must have all load-bearing observations OK and no incomplete/truncated/budget/contradiction markers; violation ⇒ downgrade to `unknown` + reason, and the roster-wide property test FAILS the build; checks stop hand-writing completeness prose — it is generated from observations | `TestEvidenceEntailsVerdict` over the full roster on profiles A/B/C + adversarial fixtures; unit tests on the predicate | Run predicate as data over `findings.profile{A,B,C}.json` and the final host artifact | OPEN |
| 2 | PRIVATE_KEY_MATERIAL_EXPOSURE passes with unreadable dirs; `WalkResult.Complete()` ignores UnreadableDirs (H1, #1, M4) | HIGH | Complete() considers only the root error | Complete() false on UnreadableDirs / CrossedMounts / budget exhaustion; adverse found ⇒ fail (proven) else incomplete ⇒ unknown with boundary list; else pass | `TestPrivateKey_UnreadableSubtreeIsUnknown`, ADV-3 (0644 key behind 0000 /root ⇒ must not pass) | Host: expected unknown (/root EACCES); Docker C: unknown | OPEN |
| 3 | PRIVATE_KEY_MATERIAL_EXPOSURE passes after zero enumerations (#2) | HIGH | pass branch does not require ≥1 inspected root | zero inspected roots ⇒ unknown ("no candidate root could be enumerated") | `TestPrivateKey_ZeroWalksIsNotPass` | Docker C (all roots unreadable) | OPEN |
| 4 | HOST_FIREWALL_STATE confident FAIL when ruleset denied / unknown implementation (#3, M1, #13) | HIGH | ruleset EACCES/EPERM/EXEC_ERROR treated as "no filtering"; `ENABLED=no` read and ignored; only a small candidate-unit list | ruleset unreadable ⇒ unknown (EACCES); `ENABLED=no` or equivalent readable disable ⇒ fail with the value cited; unknown implementations ⇒ unknown, never fail; unit state alone never proves filtering | `TestFirewall_DeniedRulesetIsUnknown`, `TestFirewall_UfwDisabledIsFail`, `TestFirewall_UnknownImplementationIsUnknown`, ADV-6 | Host: ufw ENABLED=no readable ⇒ fail; ruleset unreadable noted | OPEN |
| 5 | BMC checks claim "all enumerated successfully" over ENOENT; `!found` wins before `undetermined`; EACCES read as absence (#4, #10) | HIGH | branch ordering + prose not derived from observation statuses | absence only from OK listings of every path; any EACCES/ENOENT-of-parent/TIMEOUT among the load-bearing listings ⇒ undetermined first; prose generated from statuses (see #1) | `TestBMC_DeniedPathIsNotAbsent`, `TestBMC_ENOENTNotOverclaimed` | Docker B/C; host (node present, root-only) | OPEN |
| 6 | SSH: `sshd -G` labelled "running daemon"; confident policy verdicts while IN_FORCE says stale (#6, M3, external TDA critique) | HIGH | source label wrong; policy checks ignore the in-force result; Env caches raw output (allowed) but the conclusion "running" is not proven | label = "effective configuration as sshd would load from disk now"; policy checks (ROOT_LOGIN, AUTH_METHODS) carry `daemon_state` = the IN_FORCE observation (raw mtime/start facts, not its verdict) and downgrade to unknown/TIMESTAMP_RESOLUTION or CONTESTED when the on-disk chain is provably newer than the daemon or undecidable; no cross-check verdict state — raw facts only | `TestSSHPolicy_StaleConfigDowngradesPolicyVerdict`, `TestSSHPolicy_SourceLabelIsOnDisk` | Host: in-force undecidable (same second) ⇒ policy checks carry the caveat | OPEN |
| 7 | SSH_POLICY_IN_FORCE claims 100 ms accuracy it does not have (#7) | MEDIUM (correctness) | monotonic-to-wall reconstruction via /proc/uptime mixes clock domains; resolution asserted, not measured | resolution = max(uptime granularity 10 ms, wall-clock read skew measured across two reads, 1 s if any whole-second source) and recorded; only claim what the domains support | `TestInForce_ResolutionIsMeasuredNotAssumed` | Host artifact shows `comparison_resolution_ms` ≥ measured | OPEN |
| 8 | `Include` inside a `Match` block loses Match scope (#8) | MEDIUM (correctness) | walker flattens included directives into global scope | directives from an Include evaluated inside a Match keep that Match scope (conditional) | `TestSSHDConfig_IncludeInsideMatchStaysConditional` | fixture | OPEN |
| 9 | Truncated Include glob expansion not recorded / never load-bearing (#9) | MEDIUM (honesty) | glob expansion cap silently drops files | record `include_truncated` observation with status EXEC_ERROR-class ⇒ load-bearing ⇒ downgrade | `TestSSHDConfig_TruncatedGlobIsLoadBearing` | fixture | OPEN |
| 10 | `resolveSocketOwners` unbounded (PID×FD traversal; context never checked) (#5, external determinism note) | HIGH (boundedness) | bound lives only in the caller's context | in-operation budget: entries/syscalls/time; ctx checked per PID; boundary recorded in evidence when hit; the owner map marked incomplete ⇒ REMOTE_LISTENING_SURFACE owner attribution unknown for the remainder | `TestSocketOwners_BudgetIsEnforcedAndVisible` (fixture with thousands of fds) | Docker A run shows bounded time; evidence field present | OPEN |
| 11 | Audit other host-access paths for caller-only bounds (external §Boundedness) | MEDIUM (safety) | same class | grep every loop over /proc, /sys, dirs for in-operation budgets; add where missing | one test per added budget | — | OPEN |
| 12 | Budget arithmetic: per-check budgets sum ≈271 s vs 60 s scan deadline, sequential (external) | MEDIUM (correctness) | per-check budgets sized independently | scan deadline enforced first; per-check budget = min(declared, remaining); budget-cut emits unknown/BUDGET for the remainder (already) — add test that the sum vs deadline cannot silently starve later checks; document the arithmetic | `TestScanDeadline_DominatesPerCheckBudgets` | Host runtime ≈ 88 ms (far below) — evidence | OPEN |
| 13 | `resolveBinary` falls back to inherited PATH (L2, #12) | MEDIUM (safety) | LookPath fallback | fixed absolute candidate list only; not found ⇒ UTILITY_MISSING | `TestResolveBinary_IgnoresPATH` | Docker C | OPEN |
| 14 | ACL read follows symlinks while comment claims otherwise (L1, #11) | MEDIUM (honesty) | Getxattr vs Lgetxattr | use `syscall.Lgetxattr`; fix comment | `TestReadACL_DoesNotFollowFinalSymlink` | — | OPEN |
| 15 | MEDIA_HEALTH_VISIBILITY: reason hardcoded; children (nvme/smartctl) open device nodes (M2, external customer-safety note) | MEDIUM (safety/honesty) | exec of tools that open devices; reason not derived | remove device-opening children; derive reason from mode/owner/group vs identity + tool inventory; sysfs health signals only; distinguish "sensor never opens devices" from children | `TestMediaHealth_NoDeviceOpeningChildren`, `TestMediaHealth_ReasonDerivedFromObservations` | Host: unknown/EACCES with sysfs signals | OPEN |
| 16 | Goroutine panic containment outside runOne (external safety) | MEDIUM (safety) | `ReadOOB` goroutine and any other background goroutine lack recover | recover() in every spawned goroutine; convert to observation error | `TestOOBGoroutinePanicDoesNotKillProcess` | — | OPEN |
| 17 | Walk: incomplete/unreadable boundaries must stay visible when evidence arrays hit caps (external) | MEDIUM (honesty) | evidence array cap can drop boundary entries | boundary counters (unreadable_dirs_count, truncated=true) always emitted even when the list is capped | `TestWalkBoundaryCountersSurviveEvidenceCap` | Docker C | OPEN |
| 18 | Profile/check matrix uses t.Logf on mismatch (external, item 8) | HIGH (test gate) | contract mismatch not a failure | matrix test FAILS on mismatch; expectations = registry §5 (corrected) | `TestProfileMatrix` hard-fail | Docker A/B/C findings asserted mechanically | OPEN |
| 19 | Docker profile C not hostile (identical to A) (external, item 9) | HIGH (test gate) | only PATH changed; resolveBinary ignores PATH | rebuild C: unreadable dirs (/etc/ssh, /etc/ssl/private, /var/lib/cloud, /run/udev), absent utilities, uid with no passwd/home, masked sysfs surfaces, restricted proc where possible; assert findings | `tooling/testlab/assert_profile.py` + Dockerfile.profileC | C run shows unknowns with EACCES/UTILITY_MISSING reasons; A≠C | OPEN |
| 20 | Registry summary arithmetic (H2) | HIGH (docs) | prose not derived from table | corrected 12/8/5 (+BOOT_KERNEL_DRIFT ⇒ 12/9/5) | n/a | compare final host run | CLOSED (lead, 02:20Z) |
| 21 | CREDENTIAL_FILE_EXPOSURE system-account homes / duplicates (L3, batch-1 #5) | LOW | scope | login-capable accounts + root; dedupe; skip system dirs | `TestCredentialHomes_SkipsSystemDirsAndDeduplicates` | host: unknown (root EACCES) | TESTED (batch 1) |
| 22 | PRIVATE_KEY candidates include 121 CA-bundle symlinks (L4) | LOW | symlinks counted as candidates | regular files only; symlinks_skipped counter | `TestPrivateKey_SymlinksAreNotCandidates` | host artifact | OPEN |
| 23 | Ponytail F1 Files interface with one impl; F3 `--timeout` flag violates LD-9 and widens deadline; F5 dead decls / triplicated regexp | MEDIUM (scope/LD-9) | — | F1 concrete `*probe.Reader`; F3 remove flag, keep constant; F5 delete; `Budgeted` second interface → `Budget` field on meta (external TDA note) | main_test updated; build | — | OPEN |
| 24 | Ponytail F2 lab fixture duplication; F4 !unix stub | LOW | — | F2 DEFERRED (independence choice; another day); F4 documented as deviation 14 (only linux/amd64 ships) | — | — | DEFERRED / DOCUMENTED |
| 25 | C1 SSH_POLICY_IN_FORCE same-second false FAIL | CRITICAL | second-resolution comparison | batch 1: monotonic + TIMESTAMP_RESOLUTION | `TestSSHPolicyInForce_SameSecondIsUndecidable` (+3) | pending final host run (expected unknown) | TESTED |
| 26 | C2 PROVISIONING_DATA_PROTECTION false FAIL on redacted/public artifacts | CRITICAL | no payload/public class split | batch 1: payload-class rule | `TestProvisioning_PublicByDesignArtifactsAreNotExposure` (+3) | pending final host run (expected pass) | TESTED |
| 27 | Batch-1 #1 diskless root FAIL / #2 smartctl substring | HIGH | — | batch 1 | 2+4 tests | fixtures | TESTED |

Closure procedure: author applies fixes → regression suite (Windows + WSL uid 1000) → Staticcheck + Hydra prefilter → Docker A/B/C with mechanical assertions → fresh Fable adversarial reviewer ("make the sensor lie") → fixes → rerun everything → final Lava-host run → this table updated with runtime evidence and statuses → closure audit.
