# OBSERVATION_ANSWERS — lead-executed answers to research OBSERVATION_REQUESTs

All observations below were made by the lead through the TCI executor (read-only, bounded probes; registry `tooling/tci/probes.json`; raw output lead-only under `state/raw_host/`, sanitized copies in `state/HOST_SNAPSHOT.evidence.json`). Research agents never touched the host.

## R5 (Go architecture) — filed 2026-09-08 ~23:00Z, answered 23:05Z

| Request | Answer | Evidence (probe id) |
|---|---|---|
| R5-OR1 — the missing `finding.schema.json` | Settled by the user: no separate file exists in the supplied material; the embedded contract was transcribed into `task/derived/finding.schema.json` (draft 2020-12, validated against the metaschema; provenance in `task/derived/SCHEMA_PROVENANCE.md`). Use that file; `santhosh-tekuri/jsonschema/v6` supports 2020-12. `format: date-time` needs format assertion enabled in the validator (annotation-only by default). | n/a (derived artifact) |
| R5-OR2 — POSIX ACLs / xattrs on `/dev/ipmi0` and friends | NOT observed: the target has no `getfacl`/`getfattr`, and the recon registry forbids `python -c` by design. The sensor itself must read `system.posix_acl_access` via `syscall.Getxattr` at runtime and report `ENODATA` (no ACL) / `EACCES` / `ENOTSUP` honestly. Mode-based evidence is established: `/dev/ipmi0` `crw------- root:root` (0600). | `bmc.ipmi_dev_ls`, `bmc.ipmi_dev_perms` (getfacl: UTILITY_MISSING) |
| R5-OR3 — `/proc/self/mountinfo` + per-mount `st_dev` | Readable unprivileged. Root `/` is mount id 31, `259:2` (`/dev/nvme0n1p2`, ext4); `/boot/efi` is `259:1` vfat; `/proc`, `/sys`, `/dev`, `/run` are separate device numbers (`stat -c %d`: `/`=66306, `/boot/efi`=66305, `/proc`=24 …). So an `xdev` walk keyed on `Stat_t.Dev` of `/` correctly excludes `/proc`, `/sys`, `/dev`, `/run`, `/boot/efi`. No overlay/bind mounts of interest. | `storage.mountinfo` |
| R5-OR4 — `/sys/block` symlink shape + kernel on the target | Confirmed: every entry of `/sys/block/` is a symlink (`nvme0n1 -> ../devices/pci0000:00/…/nvme/nvme0/nvme0n1`, loops → `../devices/virtual/block/loopN`); `/sys/class/nvme/*`, `/sys/class/net/*` and even `/sys/class/dmi/id` are symlinks; `/proc/self` resolves to the pid; `/proc/1/exe` is a symlink. Kernel `6.8.0-139-generic`. A blanket `O_NOFOLLOW` under `/sys` would enumerate nothing — `os.Root` with deliberate symlink handling is required, as R5 concluded. | `storage.sysblock_ls_la`, `kernel.sysfs_symlink_shape`, `identity.uname` |
| R5-OR5 — exact `sshd_config.d` ordering and contents | One drop-in only: `/etc/ssh/sshd_config.d/00-latitude-instant-deploy.conf` (0644 root) containing `PasswordAuthentication no` and `KbdInteractiveAuthentication no`. `sshd_config` starts with `Include /etc/ssh/sshd_config.d/*.conf`, so the drop-in is processed first (first-obtained-value wins) and its values are also repeated in the main file. Effective non-comment directives listed in `state/HOST_SUMMARY.md`. | `network.sshd_config_d`, `network.sshd_effective_lines`, `network.ssh_dir_ls` |

## R2 (Lava context)
No OBSERVATION_REQUESTs filed.

## R4 (host investigation) — filed 2026-09-08 ~23:06Z, answered 23:08Z (probes added to the registry; snapshot rebuilt)

| Request | Answer | Evidence (probe id) |
|---|---|---|
| R4-OR1 — ACL on `/dev/ipmi0` | Not observable via recon (no getfacl/getfattr on host; `python -c` forbidden by the registry). Mode 0600 root:root established; the sensor reads `system.posix_acl_access` via `Getxattr` at runtime and reports ENODATA/EACCES/ENOTSUP honestly. | `bmc.ipmi_dev_ls`, `bmc.ipmi_dev_perms` |
| R4-OR2 — `/dev/i2c-*` modes | `/dev/i2c-0,1,2` are `crw------- root:root` (0600), all on the FCH SMBus controller (`0000:00:14.0`). Root-only; no group path. | `bmc.i2c_dev_modes` |
| R4-OR3 — `/etc/ufw/*` modes | Rule files (`before.rules`, `after.rules`, `user.rules`, `*6.rules`, `*.init`) are `0640 root:root` → unreadable; `/etc/ufw/ufw.conf` (0644) and `/etc/default/ufw` (0644) are readable, so `ENABLED=` and default policies (`DEFAULT_INPUT_POLICY` etc.) are observable while the concrete rule set is UNKNOWN unprivileged. | `network.ufw_file_modes` |
| R4-OR4 — AppArmor enforcement via `profiles` | `/sys/kernel/security/apparmor/profiles` is mode 0444 but reading it returns Permission denied (apparmorfs policy) → mode bits are not authorization (confirms R4-F12); enforcement mode is UNKNOWN by construction unprivileged. `/sys/module/apparmor/parameters/enabled` = Y remains the only positive evidence. | `kernel.apparmor_profiles`, `kernel.selinux_apparmor` |
| R4-OR5 — sudo policy | Deliberately NOT observed (`sudo -l` exercises the privilege path). Established: `/etc/sudoers` 0440 root:root unreadable, `/etc/sudoers.d` 0750 unreadable, group `sudo` = {ubuntu}, all other privileged groups empty (`disk`, `adm`, `systemd-journal`, `kmem`, `video`, `render`, `input`, `dialout`). Policy stays UNKNOWN; membership is the evidence. | `users.sudo_group`, `users.group_members` |
| R4-OR6 — ext4 fscrypt features | Kernel ext4 feature set (`/sys/fs/ext4/features/`): batched_discard, casefold, encrypted_casefold, encryption, fast_commit, lazy_itable_init, meta_bg_resize, metadata_csum_seed, test_dummy_encryption_v2, verity — i.e. fscrypt is *supported by the kernel*; whether the filesystem *uses* it needs the superblock (root/`dumpe2fs`) → UNKNOWN unprivileged. Per-fs `/sys/fs/ext4/nvme0n1p2/` exposes readable `errors_count`, `first_error_*`, `lifetime_write_kbytes`, `msg_count`. | `storage.ext4_features`, `storage.ext4_sysfs` |
| R4-OR7 — DMI entry attributes beyond `raw` | All attributes of `/sys/firmware/dmi/entries/38-0/` and `42-0/` (`handle`, `instance`, `length`, `position`, `type`, `raw`) are 0400 root → unreadable. Only the entry directory names prove that SMBIOS type 38 (IPMI) and 42 (Redfish host interface) records exist. | `bmc.dmi_entry_attrs` |
| R4-OR8 — `/run/udev/data` readability | Readable: `/run/udev/data/` (0755) holds 362 entries; e.g. `b259:0` exposes `S:disk/by-id/nvme-Micron_…`, `S:disk/by-diskseq/9` and `E:ID_*` properties. This is a legitimate unprivileged fallback for device model/serial/WWN when `/sys` attributes are missing (serial-bearing → treat as identity-sensitive in evidence). | `storage.udev_data_readability` |
| R4-OR9 — sshd drop-in mtime vs boot | Boot 2026-09-07 19:29:43Z; `sshd_config` mtime 2026-09-04 (image build); `sshd_config.d/00-latitude-instant-deploy.conf` mtime 2026-09-08 17:37:29.29Z, identical to `ssh.service` "Active since 17:37:29" → the drop-in was written after boot AND sshd was restarted at that moment, so the file is in force. A sensor check "config mtime newer than sshd start" is therefore meaningful and would be PASS here. | `network.sshd_dropin_mtime_vs_boot`, `network.sshd_service_status` |
| R4-OR10 — real exit code/stderr of `nvme smart-log` | `nvme smart-log /dev/nvme0n1` → stderr `Permission denied`, rc=1 (plus usage text); `nvme id-ctrl /dev/nvme0` → `Permission denied`. Device nodes: `/dev/nvme0` 0600 root:root, `/dev/ng0n1` 0600 root:root, `/dev/nvme0n1` 0660 root:disk (group empty). Classify as EACCES → UNKNOWN, never FAIL. | `storage.nvme_smart_rc`, `storage.dev_ls`, `users.group_members` |

Corrections adopted from R4 into lead artifacts: `/etc/mdadm/mdadm.conf` EXISTS (18 B, `HOMEHOST <ignore>`) — `state/HOST_SUMMARY.md` and `tooling/build_snapshot.py` fixed 23:08Z; TCI per-probe status is a coarse aggregate for multi-command probes — caveat added to HOST_SUMMARY.

## R1 (build-vs-buy) and R3 (sensor evidence) — filed ~23:10Z, answered 23:15Z (probes added; snapshot rebuilt, 193-probe registry)

| Request | Answer | Evidence (probe id) |
|---|---|---|
| **R1-OR9 (blocking)** — does `sshd -G` succeed as uid 1000? | **YES.** `sshd -G` printed the complete effective configuration (85 directives) with no error text, as uid 1000, without root and before host-key loading. Effective values include: `permitrootlogin without-password`, `passwordauthentication no`, `kbdinteractiveauthentication no`, `pubkeyauthentication yes`, `usepam yes`, `permitemptypasswords no`, `maxauthtries 6`, `logingracetime 120`, `x11forwarding yes`, `allowtcpforwarding yes`, `allowagentforwarding yes`, `authorizedkeysfile .ssh/authorized_keys .ssh/authorized_keys2`, `authorizedkeyscommand none`, `trustedusercakeys none`, `banner none`, `listenaddress 0.0.0.0:22` + `[::]:22`, `port 22`, ciphers/kex/macs lists, three `hostkey` paths. `sshd -T` in the same run exited 1 ("no hostkeys available"). The exit code I captured for `-G` (141) is a SIGPIPE artifact of my own `head -0` pipe, not sshd failing; the sensor runner must capture the real rc. Design consequence: `sshd -G` is a legitimate primary source for "in force" policy (REUSE), with the Include-aware parser as fallback when `sshd` is absent or `-G` fails; combine with the socket/unit evidence for listeners and the mtime-vs-start check for staleness. | `network.sshd_g_effective`, `network.sshd_test_config` |
| R3-OR8 — exact `sshd -T` stderr + exit code | stderr `sshd: no hostkeys available -- exiting.`, exit code 1 (unprivileged). | `network.sshd_g_effective` (rc_T=1), `network.sshd_test_config` |
| R1-OR6 — time two BMC sysfs reads | `device_id` read: ~1.41 ms; `firmware_revision`: ~1.63 ms; second `device_id`: ~0.98 ms (nanosecond `date` stamps around each `cat`). Fast here, but R1-F48/R4-F46 stand: the read is a live KCS transaction and could block for 5 s × retries on a non-answering BMC — the sensor must apply an out-of-band deadline (goroutine + timer, treat overrun as TIMEOUT/UNKNOWN). | `bmc.bmc_read_timing` |
| R1-OR7 — `ls /sys/firmware/dmi/entries` as uid 1000 | Succeeds (directory listing works; entries `38-0`, `42-0` present); all per-entry attribute files are 0400 root. readdir-as-evidence is valid; attribute reads are EACCES. | `bmc.dmi_smbios_entries`, `bmc.dmi_entry_attrs` |
| R1-OR2 / R3-OR1 — `/sys/class/dmi/id/` modes per field | 0444: bios_date, bios_release, bios_vendor, bios_version, board_asset_tag, board_name, board_vendor, board_version, chassis_asset_tag, chassis_type, chassis_vendor, product_name, product_version, product_sku, product_family, sys_vendor, modalias, uevent. 0400 root: board_serial, chassis_serial, product_serial, product_uuid. `/sys/class/dmi/id` itself is a symlink. | `identity.dmi_ls`, `identity.dmi`, `kernel.sysfs_symlink_shape` |
| R1-OR3 / R3-OR2 — ACL xattrs on `/dev/ipmi0`, `/etc/ssl/private`, `/var/log/journal`, and readability of `system.posix_acl_access` on an unreadable file | Not observable via recon (no getfacl/getfattr; `python -c` forbidden by the registry). `/var/log/journal` and `/var/log/journal/<machine-id>` carry a `+` in `ls -la` (ACL present, group systemd-journal); `/etc/ssl/private` is `drwx--x---` root:ssl-cert (no `+`). Per R1-F17 the sensor decodes `system.posix_acl_access` in pure Go; whether `getxattr` succeeds on a file the caller cannot read must be established by the sensor at runtime and reported per errno (ENODATA/EACCES/ENOTSUP). | `secrets.varlog_ls`, `services.journal_usage`, `secrets.tls_private_ls` |
| R1-OR4 — securityfs / lockdown modes | `/sys/kernel/security/lockdown` is `-rw-r--r-- root:root` (world-readable), `lsm` `-r--r--r--`; `apparmor/`, `integrity/`, `tpm0/` dirs 0755. Lockdown value `[none] integrity confidentiality`. | `kernel.securityfs_modes`, `kernel.lsm` |
| R1-OR10 / R3-OR3 — `systemctl cat ssh.socket ssh.service`; Include position + drop-in listing | `ssh.socket`: ListenStream 0.0.0.0:22 + [::]:22, Accept=no, FreeBind=yes, RequiredBy=ssh.service. `ssh.service`: `ExecStartPre=/usr/sbin/sshd -t`, `ExecStart=/usr/sbin/sshd -D $SSHD_OPTS`, `EnvironmentFile=-/etc/default/ssh`, `KillMode=process`, `Type=notify`, `Restart=on-failure`. `sshd_config` line 1 (non-comment) is `Include /etc/ssh/sshd_config.d/*.conf`; exactly one drop-in `00-latitude-instant-deploy.conf` (PasswordAuthentication no, KbdInteractiveAuthentication no). | `network.ssh_socket_cat`, `network.ssh_service_cat`, `network.sshd_effective_lines`, `network.sshd_config_d` |
| R1-OR1 / R1-OR8 — the real schema, draft, additionalProperties | Derived schema (see R5-OR1 answer): draft 2020-12, `additionalProperties` open everywhere, `reason` required for fail/unknown, integers for the three numeric fields, `format: date-time` needs assertion. | `task/derived/finding.schema.json`, `SCHEMA_PROVENANCE.md` |
| R1-OR5 — does Lava build the tarball or run our binary? | Lead decision: we ship source + a prebuilt static linux/amd64 binary + README; the documented one command runs the binary; `go build` instructions are included for reviewers with Go. The clean-room gate exercises the documented path. | n/a (decision) |
| R3-OR4 — `/run/udev/data` NVMe records | Readable; `b259:0` exposes `S:disk/by-id/…`, `S:disk/by-diskseq/9`, `E:ID_*` properties (see R4-OR8 answer). | `storage.udev_data_readability` |
| R3-OR5 — `/dev/ipmi0` mode/owner | `crw------- root:root` (0600); `/dev/ipmidev/` absent; group `disk`/`kmem` empty. | `bmc.ipmi_dev_ls`, `users.group_members` |
| R3-OR6 — both userns knobs | `kernel.unprivileged_userns_clone` = 1 (present: Ubuntu kernel), `user.max_user_namespaces` = 381532. | `users.kernel_hardening` |
| R3-OR7 — `/proc` hidepid | `/proc` mounted `rw,nosuid,nodev,noexec,relatime` with no `hidepid=` option → pid visibility unrestricted (our `ps` saw all processes). | `storage.mountinfo`, `services.ps_all` |
| R3-OR9 — `lsblk` fields on real NVMe unprivileged | `lsblk -J -O` and `-o NAME,…,SERIAL,WWN,…` populate MODEL, SERIAL, WWN (eui.…), TRAN=nvme, PHY-SEC 4096, LOG-SEC 512, STATE=live, SCHED none for both drives as uid 1000 (serials scrubbed in the shared evidence file). | `storage.lsblk_json`, `storage.lsblk_wide` |
| R3-OR10 — `st_size` pattern on the target kernel | `/proc/cpuinfo`, `/proc/meminfo`, `/proc/self/mountinfo` → size 0; `/proc/cmdline` → 225 (real); `/sys/class/dmi/id/sys_vendor`, `/sys/block/nvme0n1/size`, `/sys/devices/system/cpu/vulnerabilities/meltdown` → 4096; regular files (`/etc/os-release` 21? — see note) → real sizes. Confirms L05: never size reads from `st_size` under /proc and /sys. (Note: `/etc/os-release` on this host is a symlink to `/usr/lib/os-release`; `stat` without `-L` reported the symlink length 21.) | `kernel.stat_sizes` |
