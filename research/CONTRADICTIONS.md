# CONTRADICTIONS — every disagreement found during synthesis (2026-09-09)

Method: contradiction hunting per Vis `core/conduct/doubt-engine.md` (steelman the opposite, name concrete evidence), resolution order per this KB's rules — host evidence (`state/HOST_SNAPSHOT.evidence.json`, by probe id) beats generic guidance beats speculation; two independent primaries or one primary + LOCAL_REPRO for VERIFIED; first-seen never wins silently. Independence per `web/conduct/source-discipline.md` (same org/repo/domain = one source). Fact ids refer to `research/FACTS.jsonl`.

Status legend: **RESOLVED** (evidence decided it), **RESOLVED-HOST / OPEN-GENERIC** (host answered, mechanism still contested elsewhere), **OPEN** (needs a decision or an observation), **DECISION** (not a factual conflict — the lead decides).

| # | Kind | Disagreement | Resolution | Facts | Downstream artifacts touched |
|---|---|---|---|---|---|
| C-01 | snapshot prose vs evidence | HOST_SNAPSHOT/HOST_SUMMARY listed `mdadm.conf` as proven absent; probe `storage.mdadm_conf` printed `HOMEHOST <ignore>` | **RESOLVED**: evidence wins; `/etc/mdadm/mdadm.conf` exists (18 B), `/etc/mdadm.conf` ENOENT; lead fixed HOST_SUMMARY + build_snapshot.py 23:08Z | F2, F90 | state/HOST_SNAPSHOT.json (fixed), state/HOST_SUMMARY.md (fixed), R4 TECHNOLOGY_INVENTORY §3 |
| C-02 | tool status vs evidence | TCI per-probe `status` disagrees with per-line stdout in >=7 probes (rc read through `head`) | **RESOLVED**: per-line evidence wins; probe status never propagated; sensor records per-observation outcome | F1 | HOST_SUMMARY caveat (added); all R4 plans (already follow it) |
| C-03 | track vs track vs contract | R1-F47: `sshd -G` works before host-key loading. R3-F23 / R4-F19 / TASK_CONTRACT C4a: effective sshd config is unobtainable unprivileged, the sensor must parse | **RESOLVED by host probe** `network.sshd_g_effective`: `-G` printed 85 directives as uid 1000; `-T` exits 1. `-G` = primary oracle (REUSE), parser = fallback + provenance. R3/R4 facts about `-T` stand; their "must parse as the only path" framing retracted | F24, F26, F97 | TASK_CONTRACT C4a (already updated by the lead); still stale: R3 DESIGN_LAWS L18 ("own walker is primary"); R4 HOST_SPECIFIC_PLAN B2.4 ("do not shell out to sshd"); R4 GENERIC_FALLBACK_PLAN §7; R4 REPORT §2.3; R5 IMPL_BVB row 6; R1 BVB/REPORT ("pending R1-OR9") — see PROPAGATION_AUDIT |
| C-04 | source vs source | `sshd -G -C` documented as supported (sshd.8) vs fatal in code (sshd.c:1741-1747) | **OPEN** (source-level: code wins; NOT host-tested — the lead ran `-G` without `-C`) | F25 | OR-OPEN-1 in DECISIONS |
| C-05 | track vs track | R1: `golang.org/x/sys` REUSE (shipped, the one runtime dependency). R5: stdlib `syscall` suffices; x/sys REJECT | **RESOLVED for R5**: every needed symbol exercised in R5 LOCAL_REPRO from stdlib; the only x/sys-exclusive need (Openat2) is dropped with C-24; syscall carries a NOTE, not `Deprecated:` tags. Zero runtime third-party dependencies. Reversal: statx/openat2 flags/netlink/second GOOS | F71, F102, F96 | R1 BVB_MATRIX §3/§6 + dependency table; R1 REPORT §6 and §5a; R1 STOLEN_PATTERNS SP-13 (`unix.Getxattr`) |
| C-06 | track vs track | R1-F43: schema validation is test-only (import not linked -> zero runtime deps). R5-F58: embed schema, validate at runtime before writing, write nothing on failure | **RESOLVED (recommendation)**: test-time full validation (test-only import) + release-gate validation of the real findings.json + stdlib runtime invariants mirroring the ~10 schema constraints; NEVER suppress output on failure (write + exit non-zero + stderr pointers) because "no output" is the D3 critical failure. Reasoning: the schema is now fixed and small (F70), so drift is a compile/test-time property; R5's failure policy conflicts with D3. Lead confirms at freeze | F70, F69 | R5 ARCHITECTURE_NOTES §5 ("validate at runtime... write nothing"); R5 IMPL_BVB row 4 + "Net dependency count for the shipped binary: one"; R5 TEST_STRATEGY §5 |
| C-07 | track stream vs host | R4 stream S3: dmesg_restrict denial = EACCES. R3-F03 + host: EPERM (CAP_SYSLOG) | **RESOLVED**: EPERM (host `kernel.dmesg_head`: "Operation not permitted"); S3 claim retracted | F8, F91 | R4 GENERIC_FALLBACK_PLAN §10 wording (check) |
| C-08 | track vs host | R4-F44: `/sys/kernel/security/apparmor/profiles` is 0444 => readable without root (R4-OR4 "answerable in one read") | **RESOLVED**: REFUTED by `kernel.apparmor_profiles` — mode 0444 but `cat: Permission denied` (apparmorfs policy); enforcement mode UNKNOWN by construction; confirms R4-F12 (mode bits are not authorisation) | F50, F94 | R4 HOST_SPECIFIC_PLAN §F.4 ("(0444, R4-F44)"), R4 GENERIC_FALLBACK_PLAN §9, R4 REPORT §6, R4 PROPAGATION_NOTES R4-F44 row |
| C-09 | track vs itself | R4-F30 (only `raw` root-only) vs R4-F49 (every attribute 0400 at v6.8) | **RESOLVED by host** (R4-OR7): all six attributes 0400; directory listing is the only unprivileged signal | F4, F98 | R4 HOST_SPECIFIC_PLAN D1 (already conservative) |
| C-10 | track vs host | R1-F54 / R4 stream S3: ssh.socket ListenStream is generator-derived from sshd_config. Host `systemctl cat ssh.socket`: literal `ListenStream=0.0.0.0:22` / `[::]:22`, no drop-in shown | **RESOLVED-HOST / OPEN-GENERIC**: host unit is literal; the shipped sshd_config comment ("socket configuration must be re-generated after changing Port...") means a generator override exists on change — both readings coexist. Law is mechanism-independent: read the effective socket unit at runtime | F30 | none (R4-F31 already framed both) |
| C-11 | track vs itself | R4-F04 remediation ("no non-root account can reach the admin passthrough" implying group `disk`) vs R4-F54 (CAP_SYS_ADMIN on the 0600 char device; group disk irrelevant) | **RESOLVED**: R4-F54 + R3-F48 (kernel source) win; remediation wording retracted | F59, F100 | R4 STORAGE_ASSESSMENT §1/§4.1 item 2 ("group disk has zero members — so no unprivileged path") wording is still correct as observation; check any "join group disk" remediation text |
| C-12 | track vs track | R2-F17: "this host's BMC is not network-reachable at scan time". R4-F53: the board has a dedicated out-of-band IPMI LAN port invisible to the OS | **RESOLVED**: R4-F53 (Supermicro docs + PCI evidence) wins; OOB reachability is UNKNOWN by construction, not absent; R2-F17 retracted as stated | F47, F93 | R2 CUSTOM_CATEGORY_CANDIDATES #3 "Host reality check"; R2 REPORT Q5 |
| C-13 | brief vs research | R1 prompt: gitleaks relicensed away from MIT | **RESOLVED**: FALSE — MIT at HEAD; REJECT stands on redaction/serialisation grounds (CR-1) | F38, F92 | prompts/R1 (lead-owned); NOTES.md must not repeat it |
| C-14 | brief vs LOCAL_REPRO vs host | R1 S5 brief: securityfs root-only | **RESOLVED**: traversable (dr-xr-xr-x locally; host drwxr-xr-x, lockdown 0644, lsm 0444); lockdown ENOENT vs EACCES are different reasons (CR-2) | F50, F103 | none surviving |
| C-15 | source vs source | ghw README "No root privileges needed" (l.14) vs its own code + README l.1045-1051 | **RESOLVED**: code wins (CR-3); standing rule: README claims about unprivileged behaviour are checked against code | F63 | R1 BVB §1 (already) |
| C-16 | folklore vs docs | "exec.CommandContext handles the timeout" vs os/exec WaitDelay docs + R5 LOCAL_REPRO 30.01 s hang | **RESOLVED**: docs + repro win (CR-4) | F21, F22, F23 | none |
| C-17 | folklore vs docs + host | "Secure Boot needs root/mokutil" vs world-readable efivars + osquery + host bytes | **RESOLVED**: efivars primary, mokutil optional cross-check (CR-6) | F48 | R2 CUSTOM_CATEGORY_CANDIDATES #1 lists `mokutil --sb-state` first, "else sysfs fallback" — inverted priority (minor) |
| C-18 | source vs source | UsePAM default: upstream `no` vs Ubuntu noble packaged `yes` | **CONTESTED (generic)**; host explicit `UsePAM yes` and `-G` prints `usepam yes`, so moot here; fallback default table is distro-aware or UNKNOWN | F28 | R3 DESIGN_LAWS L19 (already PROVISIONAL-worthy) |
| C-19 | source vs source vs LOCAL_REPRO | `kernel.unprivileged_userns_clone` (Debian) vs `user.max_user_namespaces` (upstream); WSL "ubuntu" lacks the former | **RESOLVED by host** (R3-OR6): both knobs present (1 / 381532); law = probe both, never infer from distro ID | F13 | none |
| C-20 | assumption vs source | protected_* sysctls all deny EACCES? | **RESOLVED**: protected_hardlinks denies EPERM, siblings EACCES (same file, fs/namei.c) | F9 | none |
| C-21 | brief vs source | R3 brief: sshd_config and ssh_config have opposite precedence | **RESOLVED**: both first-obtained-value-wins | F27 | none |
| C-22 | doc vs doc | os.Root "containment" framing vs its own device-file caveat | **RESOLVED**: caveat wins; IsRegular gate mandatory inside a Root | F16, F19 | none |
| C-23 | track vs track | R1-F20 "lstat-first mandatory, O_NONBLOCK not sufficient" vs R5-F24 (O_NONBLOCK open of a FIFO returns immediately) and R3-F17 (stat the fd, not the path — TOCTOU) | **RESOLVED**: R1's repro used `cat` (no O_NONBLOCK) so it did not test the flag; gate = O_NONBLOCK open + fstat(fd) IsRegular; lstat optional pre-filter | F16, F95 | R1 STOLEN_PATTERNS SP-18 ("never rely on O_NONBLOCK"); R1 BVB §3 stdlib row |
| C-24 | track vs track | R1-F21 openat2 optional hardening vs R3-F18/R5-F22 (os.Root suffices; do not reach for openat2/x/sys) | **RESOLVED**: os.Root; openat2 dropped | F19, F96 | R1 BVB §3 + dependency table; R1 STOLEN_PATTERNS |
| C-25 | track vs track (status) | XCCDF nine values: R1-F57 LIKELY (NIST blocked) vs R3-F60 VERIFIED (XSD fetched) | **RESOLVED**: merged VERIFIED on R3's primary; R1's blocked fetch is not disproof | F66 | none |
| C-26 | contract vs research | TASK_CONTRACT AM-6/C6a: `ipmitool mc info` acceptable when the node is openable (hard timeout). R3 L26 + R1 BVB: never issue an IPMI command (sysfs gives the same payload; bus-disturbance risk) | **CONTESTED -> DECISION** (moot on host: node not openable). Recommendation: research position, supersede AM-6 | F104, F44, F45 | TASK_CONTRACT AM-6, C6a; CLAUDE.md core invariants ("no BMC commands beyond read-only identification" — compatible either way) |
| C-27 | contract vs research | TASK_CONTRACT B5a: `size_bytes = size x logical block size 512` vs kernel: fixed 512-byte sectors regardless of logical_block_size | **RESOLVED** (fact); contract wording stale | F55, F99 | TASK_CONTRACT B5a |
| C-28 | generic claim vs host | R3-F22: Ubuntu 24.04 cloud images ship `50-cloud-init.conf` | **WEAKENED**: absent on this Latitude image (only `00-latitude-instant-deploy.conf`); stays LIKELY generic; law rests on F27 | F84 | none |
| C-29 | research-time state vs settlement | R5-F51 / R1-OR1 / R1-OR8 / R5-OR1: schema absent, draft unknown, additionalProperties unknown | **SUPERSEDED** by AM-1 (derived schema: 2020-12, open additionalProperties) | F101, F69, F70 | R5 ARCHITECTURE_NOTES §5 "Blocked on input"; R5 IMPL_BVB 4/4b "not yet in hand"; R1 BVB §6 / REPORT §5a "whichever draft it turns out to use"; TASK_CONTRACT B6/B7/H-table still tagged PENDING-SCHEMA (AM-1 row itself is settled). CLAUDE.md line 8 already reflects the derived schema |
| C-30 | track vs track vs kickoff | Custom category: R2 ranks boot/firmware trust #1, kernel hardening #2, BMC #3, storage-reuse as the "would-not-anticipate" extra; R4 leans storage-as-data-at-rest on "unanticipated" and boot chain on evidence density; kickoff hints storage | **DECISION** (lead) — evidence laid out in DECISIONS D-01 | F48 F49 F50 F52 F58 F59 F60 F79 F80 | NOTES.md category line |
| C-31 | apparent | R1-F48: BMC sysfs read can block tens of seconds vs host timing 1-2 ms | **NOT a contradiction**: healthy path is fast, the worst case is structural (KCS retries); out-of-band deadline retained (L40) | F44 | none |
| C-32 | track vs host | R4-F18: AppArmor enforcement "unresolved, likely readable" | **RESOLVED**: unknown by construction (EACCES on profiles and parameters/mode) | F50 | R4 REPORT §6 |
| C-33 | track vs track | R1: prometheus/procfs "REUSE candidate" for DMI vs R5 IMPL_BVB row 1: BUILD + STEAL_PATTERN | **RESOLVED**: STEAL_PATTERN — large import for ~150 LOC that still lacks bounded/mode-gated reads and errno capture | F64 | R1 BVB §1 row, R1 REPORT §1/§5a "live REUSE candidate" |
| C-34 | independence | R1-F48 and R4-F46 both derive from Linux kernel source (ipmi_msghandler.c) | **RESOLVED**: counted as one source class + host observation, not two independent confirmations | F44 | none |
| C-35 | historical | TCI round-1 `is_virtual=true` from a nonempty-output rule vs `systemd-detect-virt` = none rc=1 + DMI/BMC/TPM | **RESOLVED by lead before research** (rule fixed); recorded as NOTES "Claude got wrong" candidate | F53 | none |
| C-36 | contract ambiguity | D6/AM-4 severity vs status: contract provisional rule (impact when fail/unknown, info when pass) adopted by R5; no track contradicts it | **DECISION** (confirm at freeze) — DECISIONS D-04 | — | TASK_CONTRACT D6 |
| C-37 | track claim vs host | R3-F46 / R4-F08: lsblk fstype comes from the udev DB (LIKELY on WSL) | **RESOLVED by host**: `/run/udev/data` readable, lsblk populated FSTYPE/SERIAL/WWN while the block device is unopenable (root:disk, disk empty) — the udev path is proven; empty field still != no filesystem | F57 | HOST_SUMMARY "nvme1n1: NO filesystem" should read "no filesystem signature known to udev" (wording) |
| C-38 | observation gap | ACL xattr readability on a file the caller cannot read (R3-OR2 / R1-OR3 / R5-OR2 / R4-OR1) | **OPEN**: not observable via recon (no getfacl/getfattr; python -c forbidden); settled by the sensor's first host run (ENODATA/EACCES/ENOTSUP) | F41 | OR-OPEN-2 |

## Counts
- Total: 38 entries. RESOLVED (incl. resolved-by-host, superseded, weakened, not-a-contradiction): 32. OPEN (need observation): 2 (C-04, C-38). DECISION for the lead: 3 (C-26, C-30, C-36). OPEN-GENERIC mechanism with host resolved: 1 (C-10).
- Retractions recorded in FACTS.jsonl: 14 (F90-F103 excluding F104 which is CONTESTED).

## Cross-cutting observation
Five independent artifacts (osquery secureboot, osquery disk_encryption, ipmitool errno clobber, Wazuh SCA `sshd -T`, ghw EACCES->"unknown") commit the exact failure the brief calls critical — claiming to have checked when they could not. Two of our own inputs did the same (C-13, C-14) and two of our research tracks did too (C-08, C-07). The pattern is the strongest argument that UNKNOWN semantics are the engineering content of this exercise (F65).
