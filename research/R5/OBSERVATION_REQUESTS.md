# OBSERVATION_REQUESTS — R5

Research agents do not observe the host. Each entry is a request to the lead's TCI executor. R5 proceeded under the labelled assumption stated in each entry.

```json
{"id":"R5-OR1","question":"Please supply task/original/finding.schema.json (the real file from Lava), or confirm it has not been provided.","why_it_matters":"Question 5 asks for a validation strategy AND a library choice. The library choice is decidable from conformance data without the file (R5-F52), but the declared JSON Schema dialect ($schema) determines whether draft-07 vs 2019-09 vs 2020-12 support is the binding requirement, and additionalProperties/required determine whether extra evidence fields and our unknown marker are legal at all (B7, D4).","acceptable_evidence":"The file itself in task/original/, or a statement that Lava has not sent it.","suggested_safe_probe":null,"blocking":false}
{"id":"R5-OR2","question":"Does /dev/ipmi0 carry a POSIX ACL, and do /etc/ssl/private, /etc/shadow or /dev/i2c-* carry one? getfacl is absent, so please read the xattr directly: python3 -c \"import os;\\nfor p in ['/dev/ipmi0','/etc/shadow','/etc/ssl/private','/dev/tpm0']:\\n  try: print(p, os.listxattr(p))\\n  except OSError as e: print(p,'ERR',e.errno)\"","why_it_matters":"BMC_INBAND_ACCESS must answer 'who is permitted to talk to the BMC'. Mode 0600 root:root is the visible answer, but an ACL can grant additional users and would change the finding from 'only root' to 'root plus <entries>'. It also decides whether the ~50-line ACL decoder (R5-F27) is load-bearing on this host or purely generic-fallback code.","acceptable_evidence":"The xattr name list per path (names only, no values needed), or ENODATA/EACCES per path.","suggested_safe_probe":"python3 -c \"import os\\nfor p in ('/dev/ipmi0','/etc/shadow','/etc/ssl/private','/dev/tpm0','/dev/i2c-0'):\\n    try: print(p, os.listxattr(p))\\n    except OSError as e: print(p, 'ERRNO', e.errno)\"","blocking":false}
{"id":"R5-OR3","question":"Please capture /proc/self/mountinfo (full) and the st_dev of /, /proc, /sys, /run, /tmp, /boot/efi on the target.","why_it_matters":"The bounded secrets walk uses xdev via syscall.Stat_t.Dev (R5-F25). Sizing the walk budget and choosing the prune list needs the real mount count and the real device IDs; a surprise bind mount or an unexpected tmpfs changes both. The LOCAL_REPRO mount layout is WSL2, which is not representative.","acceptable_evidence":"cat /proc/self/mountinfo; stat -c '%n %d' / /proc /sys /run /tmp /boot/efi","suggested_safe_probe":"cat /proc/self/mountinfo; stat -c '%n dev=%d' / /proc /sys /run /tmp /boot/efi","blocking":false}
{"id":"R5-OR4","question":"Confirm os.Root/openat2 behaviour on the target kernel by checking that /sys/block entries are symlinks there as they are locally: ls -la /sys/block | head -20; readlink /sys/block/nvme0n1; uname -r","why_it_matters":"R5-F21/F22 (all /sys/block entries are symlinks; os.Root follows in-root symlinks) were established on kernel 6.18 WSL2. The target runs 6.8.0-139 with 7.0.0-31 pending. openat2 has existed since 5.6 so drift is unlikely, but the block-device enumeration design depends on this and the sensor must degrade to plain bounded reads if os.OpenRoot fails.","acceptable_evidence":"ls -la /sys/block output plus uname -r.","suggested_safe_probe":"uname -r; ls -la /sys/block; readlink /sys/block/nvme0n1","blocking":false}
{"id":"R5-OR5","question":"Confirm the exact content and ordering of /etc/ssh/sshd_config.d/*.conf (filenames plus non-comment directives) and the first 5 lines of /etc/ssh/sshd_config.","why_it_matters":"sshd_config first-value-wins plus lexical Include ordering (R5-F43) means the effective value can come from a drop-in that sorts before another. The snapshot records that 00-latitude-instant-deploy.conf restates PasswordAuthentication, but the parser's correctness test needs the real ordering as a fixture.","acceptable_evidence":"ls -1 /etc/ssh/sshd_config.d/ plus grep -v '^#' output per file.","suggested_safe_probe":"ls -1 /etc/ssh/sshd_config.d/; head -5 /etc/ssh/sshd_config; grep -rvh -e '^#' -e '^$' /etc/ssh/sshd_config.d/","blocking":false}
```

**Assumptions held in the meantime (labelled):**

* **A-OR1** — the schema declares draft 2020-12 or draft-07 and permits additional properties. Both are covered by the recommended validator (R5-F52), so the library choice is robust to the answer; only the field shapes are deferred.
* **A-OR2** — `/dev/ipmi0` carries no ACL and mode 0600 root:root is the whole answer. The ACL decoder is written anyway as generic fallback; if the assumption holds, the BMC check's evidence simply records `ENODATA`.
* **A-OR3** — the target's mount set is close to the snapshot (root ext4 + EFI vfat + the usual pseudo-filesystems), so a walk budget of a few tens of thousands of entries with a prune list is adequate.
* **A-OR4** — sysfs on 6.8/7.0 has the same symlink shape as locally observed, and `os.OpenRoot` succeeds.
* **A-OR5** — the drop-in restates rather than contradicts the main file, so the effective values match the snapshot's `sshd -T`-free derivation.
